package com.me;

public enum rules {

    /***
     * Enum Usage
     *
     * MATCHED : Rule Matches, Hence can't be created.
     * NOT_MATCHED : Rule is unique
     * INVALID : Rule Created is Invalid
     * VALID : Rule Created is Valid
     */
    MATCHED, NOT_MATCHED, INVALID, VALID
}
